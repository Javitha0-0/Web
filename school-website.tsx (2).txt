import React, { useState, useEffect } from 'react';
import { AlertCircle, Mail, Phone, MapPin, Camera, Menu, X } from 'lucide-react';
import { Alert, AlertDescription } from '@/components/ui/alert';

const App = () => {
  const [currentPage, setCurrentPage] = useState('home');
  const [showCookieAlert, setShowCookieAlert] = useState(true);
  const [isMenuOpen, setIsMenuOpen] = useState(false);

  // Prevent right-click
  useEffect(() => {
    const handleContextMenu = (e) => {
      e.preventDefault();
    };
    document.addEventListener('contextmenu', handleContextMenu);
    return () => {
      document.removeEventListener('contextmenu', handleContextMenu);
    };
  }, []);

  // Disable developer tools shortcuts
  useEffect(() => {
    const handleKeyPress = (e) => {
      if (
        (e.ctrlKey && e.shiftKey && e.key === 'I') ||
        (e.ctrlKey && e.shiftKey && e.key === 'J') ||
        (e.key === 'F12')
      ) {
        e.preventDefault();
      }
    };
    document.addEventListener('keydown', handleKeyPress);
    return () => {
      document.removeEventListener('keydown', handleKeyPress);
    };
  }, []);

  const Navigation = () => (
    <nav className="bg-red-700 text-white p-4">
      <div className="container mx-auto flex justify-between items-center">
        <h1 className="text-2xl font-bold">Homagama Maha Vidyalaya</h1>
        <div className="lg:hidden">
          <button onClick={() => setIsMenuOpen(!isMenuOpen)}>
            {isMenuOpen ? <X size={24} /> : <Menu size={24} />}
          </button>
        </div>
        <div className={`lg:flex gap-6 ${isMenuOpen ? 'block absolute top-16 left-0 right-0 bg-red-700 p-4' : 'hidden lg:block'}`}>
          {['home', 'about', 'gallery', 'contact'].map((page) => (
            <button
              key={page}
              onClick={() => {
                setCurrentPage(page);
                setIsMenuOpen(false);
              }}
              className={`capitalize ${
                currentPage === page ? 'text-yellow-300' : 'hover:text-yellow-200'
              }`}
            >
              {page}
            </button>
          ))}
        </div>
      </div>
    </nav>
  );

  const HomePage = () => (
    <div className="min-h-screen">
      <div className="relative h-96">
        <img
          src="/api/placeholder/1200/400"
          alt="School building"
          className="w-full h-full object-cover"
        />
        <div className="absolute inset-0 bg-black bg-opacity-50 flex items-center justify-center">
          <div className="text-center text-white">
            <h1 className="text-4xl md:text-6xl font-bold mb-4">Welcome to</h1>
            <h2 className="text-3xl md:text-5xl font-bold mb-4">Homagama Maha Vidyalaya</h2>
            <p className="text-xl">Nurturing Tomorrow's Leaders</p>
          </div>
        </div>
      </div>
      <div className="container mx-auto py-12 px-4">
        <div className="grid md:grid-cols-3 gap-8">
          <div className="bg-red-100 p-6 rounded-lg">
            <h3 className="text-xl font-bold mb-4">Academic Excellence</h3>
            <p>Committed to providing quality education and fostering academic growth.</p>
          </div>
          <div className="bg-yellow-100 p-6 rounded-lg">
            <h3 className="text-xl font-bold mb-4">Sports & Activities</h3>
            <p>Comprehensive sports programs and extracurricular activities for holistic development.</p>
          </div>
          <div className="bg-red-100 p-6 rounded-lg">
            <h3 className="text-xl font-bold mb-4">Modern Facilities</h3>
            <p>State-of-the-art infrastructure supporting modern learning needs.</p>
          </div>
        </div>
      </div>
    </div>
  );

  const AboutPage = () => (
    <div className="container mx-auto py-12 px-4">
      <h2 className="text-3xl font-bold mb-8">About Our School</h2>
      <div className="grid md:grid-cols-2 gap-8">
        <div>
          <img
            src="/api/placeholder/600/400"
            alt="School campus"
            className="rounded-lg"
          />
        </div>
        <div>
          <h3 className="text-2xl font-bold mb-4">Our Vision</h3>
          <p className="mb-4">
            To be a leading educational institution that nurtures academic excellence,
            character development, and innovative thinking.
          </p>
          <h3 className="text-2xl font-bold mb-4">Our Mission</h3>
          <p className="mb-4">
            To provide quality education that empowers students to become responsible
            citizens and future leaders through comprehensive learning experiences.
          </p>
        </div>
      </div>
    </div>
  );

  const GalleryPage = () => (
    <div className="container mx-auto py-12 px-4">
      <h2 className="text-3xl font-bold mb-8">School Gallery</h2>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {[...Array(6)].map((_, index) => (
          <div key={index} className="relative group">
            <img
              src={`/api/placeholder/400/${300 + index}`}
              alt={`Gallery image ${index + 1}`}
              className="w-full h-64 object-cover rounded-lg transition-transform group-hover:scale-105"
            />
            <div className="absolute inset-0 bg-black bg-opacity-0 group-hover:bg-opacity-50 transition-opacity flex items-center justify-center">
              <Camera className="text-white opacity-0 group-hover:opacity-100 transition-opacity" size={32} />
            </div>
          </div>
        ))}
      </div>
    </div>
  );

  const ContactPage = () => (
    <div className="container mx-auto py-12 px-4">
      <h2 className="text-3xl font-bold mb-8">Contact Us</h2>
      <div className="grid md:grid-cols-2 gap-8">
        <div>
          <div className="mb-6">
            <h3 className="text-xl font-bold mb-4">Get in Touch</h3>
            <div className="space-y-4">
              <div className="flex items-center gap-2">
                <Mail size={20} />
                <span>info@homagamamv.edu.lk</span>
              </div>
              <div className="flex items-center gap-2">
                <Phone size={20} />
                <span>+94 11 2855XXX</span>
              </div>
              <div className="flex items-center gap-2">
                <MapPin size={20} />
                <span>Homagama, Sri Lanka</span>
              </div>
            </div>
          </div>
        </div>
        <div>
          <form className="space-y-4">
            <div>
              <label className="block mb-2">Name</label>
              <input type="text" className="w-full p-2 border rounded" />
            </div>
            <div>
              <label className="block mb-2">Email</label>
              <input type="email" className="w-full p-2 border rounded" />
            </div>
            <div>
              <label className="block mb-2">Message</label>
              <textarea className="w-full p-2 border rounded h-32"></textarea>
            </div>
            <button className="bg-red-700 text-white px-6 py-2 rounded hover:bg-red-800">
              Send Message
            </button>
          </form>
        </div>
      </div>
    </div>
  );

  return (
    <div className="min-h-screen bg-gray-50">
      <Navigation />
      
      {showCookieAlert && (
        <Alert className="fixed bottom-0 left-0 right-0 z-50 bg-white border-t">
          <AlertCircle className="h-4 w-4" />
          <AlertDescription>
            <div className="flex items-center justify-between">
              <span>We use cookies to enhance your browsing experience.</span>
              <button
                onClick={() => setShowCookieAlert(false)}
                className="bg-red-700 text-white px-4 py-2 rounded text-sm"
              >
                Accept
              </button>
            </div>
          </AlertDescription>
        </Alert>
      )}

      {currentPage === 'home' && <HomePage />}
      {currentPage === 'about' && <AboutPage />}
      {currentPage === 'gallery' && <GalleryPage />}
      {currentPage === 'contact' && <ContactPage />}

      <footer className="bg-red-800 text-white py-8">
        <div className="container mx-auto px-4">
          <div className="grid md:grid-cols-3 gap-8">
            <div>
              <h3 className="text-xl font-bold mb-4">Homagama Maha Vidyalaya</h3>
              <p>Excellence in Education Since 1950</p>
            </div>
            <div>
              <h3 className="text-xl font-bold mb-4">Quick Links</h3>
              <ul className="space-y-2">
                {['Admissions', 'Academic Calendar', 'News & Events', 'Student Portal'].map((link) => (
                  <li key={link}>
                    <button className="hover:text-yellow-200">{link}</button>
                  </li>
                ))}
              </ul>
            </div>
            <div>
              <h3 className="text-xl font-bold mb-4">Contact Info</h3>
              <p>Homagama, Sri Lanka</p>
              <p>Phone: +94 11 2855XXX</p>
              <p>Email: info@homagamamv.edu.lk</p>
            </div>
          </div>
          <div className="mt-8 pt-8 border-t border-red-700 text-center">
            <p>&copy; 2024 Homagama Maha Vidyalaya. All rights reserved.</p>
          </div>
        </div>
      </footer>
    </div>
  );
};

export default App;
